using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class trigger_effect : MonoBehaviour
{

    GameObject effects;
    GameObject animate;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
      //  if (effects.GetComponent<turret_animator>().active == true)
      //  {
      //      animate.SetActive(true);
      //  }
    }
}
